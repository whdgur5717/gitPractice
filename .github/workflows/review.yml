name: Cache Test

on: push

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Test
        uses: whdgur5717/actions@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
